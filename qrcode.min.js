/* qrcode.min.js — PLACEHOLDER (QR generation is pinned for a later step)
   - index.html loads this with <script src defer>; the stub keeps that a 200,
     not a 404.
   - It intentionally does NOT define window.QRCode. app.js's renderQRBlock()
     checks `typeof window.QRCode === 'function'`; since that's false, it HIDES
     the entire .ballot-card__qr region — on screen and on the printed card. No
     empty bordered box, no "Scan to reopen your choices" label with nothing to
     scan, and no focusable fallback link stranded inside an aria-hidden
     container. The resume URL is still available through "Copy as text" (and, in
     the no-localStorage fallback mode, printed in the card footer).

   To un-pin later: REPLACE this file with ONE vendored, MIT, client-side
   generator. No CDN and no QR-image API — either would violate the
   "zero external requests" posture and the strict CSP.

   ⚠ CSP CAUTION when choosing one: index.html sets `default-src 'self'` with no
   `style-src` and no 'unsafe-inline', so any library path that writes inline
   style="" attributes will be blocked by the browser.
     - davidshimjs/qrcodejs          → matches app.js's current adapter API
                                       as-is, but its <table> fallback path sets
                                       inline styles. Verify which path runs.
     - kazuhikoarase/qrcode-generator → smaller, emits crisp SVG or a
                                       data:-URI <img> (data: is already allowed
                                       by img-src) with no style attributes.
                                       PREFERRED. renderQRBlock() is the only
                                       function that needs changing.
   Test with the console open and confirm zero CSP violation reports, then check
   a print preview: the code must render as solid black modules at 3cm.
*/
/* (intentionally empty — no global exported on purpose) */
