/* qrcode.min.js — PLACEHOLDER (QR generation is pinned for a later step)

   - index.html loads this with <script src defer>; the stub keeps that a 200,
     not a 404.
   - It intentionally does NOT define window.QRCode. app.js's renderQR() checks
     `typeof window.QRCode === 'function'`; since that's false, it degrades to a
     plain "Open your resume link" anchor — a valid POC behavior that still
     carries the choices-only resume URL (§6/§9.4).

   To un-pin later: REPLACE this file with ONE vendored, MIT, client-side
   generator (no CDN / no image API — that would violate constraints #2/#3):
     - davidshimjs/qrcodejs         → matches app.js's current adapter as-is.
     - kazuhikoarase/qrcode-generator → smaller, crisp SVG (prints sharper);
                                        renderQR() is the only fn to change.
*/
/* (intentionally empty — no global exported on purpose) */